{% extends 'user.html.twig' %}

{% block title %}Add announce{% endblock %}

{% block body %}
    <h1>Add an announce</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}
    <input type="submit" value="Guardar" class="btn btn-success" />
    {{ form_end(form) }}
{% endblock %}
